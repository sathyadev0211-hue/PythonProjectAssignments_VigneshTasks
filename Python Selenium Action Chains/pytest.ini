[pytest]
# pytest.ini – project-level Pytest configuration

# Always generate an HTML report named "report.html" in the project root
addopts = --html=report.html --self-contained-html -v

# Display test execution in verbose mode
log_cli = true
log_cli_level = INFO
